entryScene: cover
sceneSize: { width: 2560, height: 1440 }

fonts:
  default:
    font: "30px Open Sans"
    color: "#000000"

sprites:
  next_button: "images/next_button.png"
  prev_button: "images/prev_button.png"

controls:
  next_scene_control:
    sprite: next_button
    position: { x: 2290, y: 1280 }

    triggers:
      onClick:
        action: nextScene

  prev_scene_control:
    sprite: prev_button
    position: { x: 210, y: 1280 }

    triggers:
      onClick:
        action: prevScene
